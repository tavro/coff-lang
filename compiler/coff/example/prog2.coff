prog main: {

con SIZE = 1000;

var list: array[SIZE] real;

subprog:
    func half(foo: int; bar: int): int {
        return foo / bar;
    }

    proc loop(stop: int; start: int; val: int) {
        var i: int;
        // TODO ifstatement & loop
    }

    func check: int {
        ret 1;
    }

    func zero: int {
        ret 0;
    }

// TODO call functions and procedures
}